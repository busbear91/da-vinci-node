# === Dependencies ===
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# === Next.js build output ===
.next/
out/
build/
dist/

# === Production ===
*.tsbuildinfo
next-env.d.ts

# === Environment variables ===
# NEVER commit these — they contain SUPABASE_SERVICE_ROLE_KEY
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Keep the template so teammates know which vars exist
!.env.example

# === Logs ===
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
logs/
*.log

# === Editor / OS ===
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# === Vercel ===
.vercel

# === TypeScript ===
*.tsbuildinfo

# === Supabase local dev ===
# If you use `supabase start` for a local stack, it drops these
supabase/.branches
supabase/.temp
supabase/.env

# === Cloudflared credentials ===
# In case anyone ever copies a tunnel credentials JSON into the repo by mistake
*.cloudflared.json
ce